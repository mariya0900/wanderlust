/**
 * import 'package:flutter/material.dart';

// This is based on the class code so I'm not sure if it completely does what we
// want it to

// ref exercise 8

class SetNotificationWidget extends StatefulWidget {
  const SetNotificationWidget({Key? key}) : super(key: key);

  @override
  _SetNotificationWidgetState createState() => _SetNotificationWidgetState();
}

class _SetNotificationWidgetState extends State<SetNotificationWidget> {
  
}
 */